<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Connecta_IPBVC.Views.AnexosPage"
             Title="AnexosPage">
	<ScrollView>
		<VerticalStackLayout Padding="20" Spacing="15">

            <CollectionView ItemsSource="{Binding Anexos}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="10" BackgroundColor="#eeeeee" CornerRadius="10">
                            <VerticalStackLayout Spacing="5">

                                <Label Text="{Binding NomeArquivoOriginal}"
                           FontSize="18" />

                                <Button Text="Abrir"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:AnexosPage}}, Path=BindingContext.AbrirAnexoCommand}"
                            CommandParameter="{Binding Url}" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>


        </VerticalStackLayout>
	</ScrollView>
</ContentPage>